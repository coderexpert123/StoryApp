<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".shareanimation"></activity>
        <activity android:name=".dashboard" />
        <activity android:name=".setting" />
        <activity android:name=".forget" />
        <activity android:name=".signup" />
        <activity android:name=".login" />
        <activity android:name=".recipebama" android:parentActivityName=".dashboard"  />
        <activity android:name=".update" />
        <activity android:name=".upload" />
        <activity android:name=".detail" android:parentActivityName=".recipebama" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>